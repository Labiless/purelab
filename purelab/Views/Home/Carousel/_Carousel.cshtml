@model List<purelab.Models.Post>
@using System.Linq
@{
    Post ce1 = Model.Where(post => post.Active == true).ToList()[0];
    Post ce2 = Model.Where(post => post.ImageUrl == "img/carousel_2.jpg").ToList()[0];
}


<div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel">
    <div class="carousel-inner">
        <partial name="Carousel/_CarouselElement" model="ce1"/>
        <partial name="Carousel/_CarouselElement" model="ce2" />
    </div>
    <!--control-->
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
    </button>
</div>